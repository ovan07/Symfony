<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
        {% block stylesheets %}
        {% endblock %}

        {% block javascripts %}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}
    </head>
    <body class="font-sans bg-gray-100 text-gray-800 leading-relaxed flex min-h-screen m-0">
        <nav class="w-55 min-w-55 bg-sidebar text-white min-h-screen py-6 flex flex-col">
            <div class="text-xl font-bold px-5 pb-5 border-b border-white/10 mb-2">My App</div>
            <a href="{{ path('home') }}" class="block py-2.5 px-5 text-white/70 no-underline text-[0.95rem] transition-colors duration-200 hover:bg-white/8 hover:text-white {{ app.request.pathInfo == '/' ? 'bg-primary/25 text-white border-l-3 border-primary' }}">Home</a>
            <a href="{{ path('product_index') }}" class="block py-2.5 px-5 text-white/70 no-underline text-[0.95rem] transition-colors duration-200 hover:bg-white/8 hover:text-white {{ app.request.pathInfo starts with '/product' ? 'bg-primary/25 text-white border-l-3 border-primary' }}">Products</a>
            <a href="{{ path('category_index') }}" class="block py-2.5 px-5 text-white/70 no-underline text-[0.95rem] transition-colors duration-200 hover:bg-white/8 hover:text-white {{ app.request.pathInfo starts with '/category' ? 'bg-primary/25 text-white border-l-3 border-primary' }}">Categories</a>
        </nav>
        <div class="flex-1 py-8 px-10 max-w-[960px]">
            {% block body %}{% endblock %}
        </div>
    </body>
</html>
