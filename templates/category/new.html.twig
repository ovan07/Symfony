{% extends 'base.html.twig' %}

{% block title %}New Category{% endblock %}

{% block body %}
    <h1 class="text-3xl font-bold mb-5 text-sidebar">Create New Category</h1>

    {% include 'category/_form.html.twig' with {button_label: 'Create'} %}

    <div class="my-5 flex gap-2 items-center">
        <a href="{{ path('category_index') }}" class="btn btn-secondary">Back to list</a>
    </div>
{% endblock %}
